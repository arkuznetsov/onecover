
Процедура ПередЗаписью(Отказ)
	
	Если НЕ ЗначениеЗаполнено(Наименование) Тогда
		Наименование = СокрЛП(ТипСервиса) + ": "
					 + ?(ТипСервиса = Перечисления.ктв_сб_ТипСервиса.ХТТП, СокрЛП(Сервер), "")
					 + СокрЛП(АдресРесурса);
	КонецЕсли;
			 
КонецПроцедуры //ПередЗаписью()
